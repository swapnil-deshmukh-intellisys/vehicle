import React from 'react';

describe('SelectBikeStep', () => {
  test('renders without crashing', () => {
    expect(true).toBe(true);
  });

  test('component exists', () => {
    expect(true).toBe(true);
  });

  test('can be tested', () => {
    expect(typeof React).toBe('object');
  });
});
